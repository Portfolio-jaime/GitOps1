apiVersion: v1
kind: Service
metadata:
  name: {{ include "gitops1.fullname" . }}
  labels:
    app: {{ include "gitops1.name" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
  selector:
    app: {{ include "gitops1.name" . }}
